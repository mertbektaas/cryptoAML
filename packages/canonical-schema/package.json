{
  "name": "@crypto-aml/canonical-schema",
  "version": "1.0.0",
  "description": "Canonical schema definitions for Address, Transaction, Token, Smart Contract, and Cross-chain bridge entities",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "test": "node --test dist/**/*.test.js"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
